package winw.ai.neuralnetwork;

/**
 * 前馈神经网络（Feed forward Neural Network）
 * <p>
 * 信息从输入层开始输入，每层的神经元接收前一级输入，并输出到下一级，直至输出层。整个网络信息输入传输中无反馈（循环）。即任何层的输出都不会影响同级层，可用一个有向无环图表示。
 * 
 * 常见的前馈神经网络包括卷积神经网络（CNN）、全连接神经网络（FCN)、生成对抗网络(GAN)等。
 */

public class FeedforwardNeuralNetwork {// 例如出现 某些特征后，在神经网络中可以计算出99%是 某个物体。

}
